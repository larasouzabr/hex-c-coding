#include <stdio.h>
/**
*main - prints all single digit numbers of base 10 starting from 0
*Return: It returns 0
*/
int main(void)
{
int number;
for (number = 0; number < 10; number++)
printf("%i", number);
printf("\n");
return (0);
}
